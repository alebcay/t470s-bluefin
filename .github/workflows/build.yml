---
name: Build container image
on:
  pull_request:
    branches:
      - main
  schedule:
    - cron: '05 10 * * *'  # 10:05am UTC everyday
  push:
    branches:
      - main
    paths-ignore:
      - '**/README.md'
  workflow_dispatch:

env:
  IMAGE_DESC: "My Customized Universal Blue Image"
  IMAGE_KEYWORDS: "bootc,ublue,universal-blue"
  IMAGE_LOGO_URL: "https://avatars.githubusercontent.com/u/120078124?s=200&v=4"  # Put your own image here for a fancy profile on https://artifacthub.io/!
  IMAGE_NAME: "${{ github.event.repository.name }}"  # output image name, usually same as repo name
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"  # do not edit
  DEFAULT_TAG: "latest"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ inputs.brand_name}}-${{ inputs.stream_name }}
  cancel-in-progress: true

jobs:
  # build_kernel:
  #   name: Build kernel RPM
  #   runs-on: ubuntu-24.04
  #   container:
  #     image: ghcr.io/ublue-os/bluefin:stable

  #   permissions:
  #     contents: read
  #     actions: write

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
  #       with:
  #         fetch-depth: 0
  #         persist-credentials: false

  #     - name: Check if build-kernel.sh changed
  #       id: script_changed
  #       shell: bash
  #       env:
  #         BEFORE: ${{ github.event.before }}
  #         AFTER: ${{ github.event.after }}
  #         GH_API_URL: ${{ github.api_url }}
  #         GH_REPOSITORY: ${{ github.repository }}
  #         GH_EVENT_PULL_REQUEST_NUMBER: ${{ github.event.pull_request.number }}
  #       run: |
  #         changed=false
  #         case "${{ github.event_name }}" in
  #           push)
  #             if [ -n "$BEFORE" ] && [ "$BEFORE" != "0000000000000000000000000000000000000000" ]; then
  #               if curl -sL \
  #                 -H "Authorization: Bearer ${{ github.token }}" \
  #                 -H "Accept: application/vnd.github+json" \
  #                 "${GH_API_URL}/repos/${GH_REPOSITORY}/compare/${BEFORE}...${AFTER}" \
  #                 | jq -e '.files | any(.filename == "build_files/build-kernel.sh")' >/dev/null; then
  #                 changed=true
  #               fi
  #             fi
  #             ;;
  #           pull_request)
  #             if curl -sL \
  #               -H "Authorization: Bearer ${{ github.token }}" \
  #               -H "Accept: application/vnd.github+json" \
  #               "${GH_API_URL}/repos/${GH_REPOSITORY}/pulls/${GH_EVENT_PULL_REQUEST_NUMBER}/files" \
  #               | jq -e 'any(.filename == "build_files/build-kernel.sh")' >/dev/null; then
  #               changed=true
  #             fi
  #             ;;
  #         esac
  #         echo "changed=${changed}" >> "$GITHUB_OUTPUT"

  #     - name: Query kernel version from COPR
  #       id: kernel_ver
  #       shell: bash
  #       run: |
  #         echo "version=$(./build_files/build-kernel.sh query-version)" >> "$GITHUB_OUTPUT"

  #     - name: Restore cached kernel RPMs
  #       id: cache_kernel
  #       uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
  #       with:
  #         path: /tmp/kernel-rpms
  #         key: kernel-cachyos-lto-${{ steps.kernel_ver.outputs.version }}

  #     - name: Build kernel RPM from source
  #       if: steps.cache_kernel.outputs.cache-hit != 'true' || steps.script_changed.outputs.changed == 'true'
  #       shell: bash
  #       run: ./build_files/build-kernel.sh build

  #     - name: Delete old kernel RPM cache before saving
  #       if: (steps.cache_kernel.outputs.cache-hit != 'true' || steps.script_changed.outputs.changed == 'true') && github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
  #       shell: bash
  #       env:
  #         GH_API_URL: ${{ github.api_url }}
  #         GH_REPOSITORY: ${{ github.repository }}
  #         GH_TOKEN: ${{ github.token }}
  #         KERNEL_VERSION: ${{ steps.kernel_ver.outputs.version }}
  #       run: |
  #         KEY="kernel-cachyos-lto-${KERNEL_VERSION}"
  #         curl -sL \
  #           -X DELETE \
  #           -H "Authorization: Bearer ${GH_TOKEN}" \
  #           -H "Accept: application/vnd.github+json" \
  #           "${GH_API_URL}/repos/${GH_REPOSITORY}/actions/caches?key=${KEY}" 2>/dev/null || true

  #     - name: Save kernel RPMs to cache
  #       if: (steps.cache_kernel.outputs.cache-hit != 'true' || steps.script_changed.outputs.changed == 'true') && github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
  #       uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
  #       with:
  #         path: /tmp/kernel-rpms
  #         key: kernel-cachyos-lto-${{ steps.kernel_ver.outputs.version }}

  #     - name: Upload kernel RPMs
  #       uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
  #       with:
  #         name: kernel-rpms
  #         path: /tmp/kernel-rpms/

  build_push:
    name: Build and push image
    runs-on: ubuntu-latest
    # needs: build_kernel

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Prepare environment
        run: |
          # Lowercase the image uri
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}

      # These stage versions are pinned by https://github.com/renovatebot/renovate
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      # This is optional, but if you see that your builds are way too big for the runners, you can enable this by uncommenting the following lines:
      - name: Maximize build space
        uses: ublue-os/remove-unwanted-software@695eb75bc387dbcd9685a8e72d23439d8686cba6

      - name: Set SOURCE_DATE_EPOCH
        run: echo "SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)" >> $GITHUB_ENV

      # Image metadata for https://artifacthub.io/ - This is optional but is highly recommended so we all can get a index of all the custom images
      # The metadata by itself is not going to do anything, you choose if you want your image to be on ArtifactHub or not.
      - name: Image Metadata
        uses: docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9 # v6.1.0
        id: metadata
        with:
          # This generates all the tags for your image, you can add custom tags here too!
          # Default tags are "$DEFAULT_TAG" and "$DEFAULT_TAG.$date".
          tags: |
            type=raw,value=${{ env.DEFAULT_TAG }}
            type=raw,value=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
            type=raw,value={{date 'YYYYMMDD'}}
            type=sha,enable=${{ github.event_name == 'pull_request' }}
            type=ref,event=pr
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.created={{date 'YYYY-MM-DDTHH:mm:ssZ'}}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/blob/${{ github.sha }}/Containerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/tree/${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ env.DEFAULT_TAG }}.{{date 'YYYYMMDD'}}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.IMAGE_LOGO_URL }}
            io.artifacthub.package.prerelease=false
            containers.bootc=1
          sep-tags: " "
          sep-annotations: " "

      # - name: Download kernel RPMs
      #   uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
      #   with:
      #     name: kernel-rpms
      #     path: build_files/kernel-rpms/

      - name: Build Image
        id: build_image
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2.13
        with:
          containerfiles: |
            ./Containerfile
          # Postfix image name with -custom to make it a little more descriptive
          # Syntax: https://docs.github.com/en/actions/learn-github-actions/expressions#format
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          oci: true
          extra-args: |
            --timestamp ${{ env.SOURCE_DATE_EPOCH }}
            --build-arg BUILD_SOURCE_DATE_EPOCH=${{ env.SOURCE_DATE_EPOCH }}

      - name: Rechunk Image
        id: rechunk_image
        run: |
          podman inspect "${IMAGE_NAME}:${DEFAULT_TAG}" > chunkah_config.json
          podman run --rm \
            --mount=type=image,src="${IMAGE_NAME}:${DEFAULT_TAG}",target=/chunkah \
            --mount=type=bind,src=./chunkah_config.json,target=/tmp/chunkah_config.json,relabel=shared \
            -e SOURCE_DATE_EPOCH \
            quay.io/coreos/chunkah:v0.6.0 \
            build \
            --verbose \
            --compressed \
            --max-layers 128 \
            --prune /sysroot/ \
            --config /tmp/chunkah_config.json \
            --label ostree.commit- --label ostree.final-diffid- \
            --tag "${IMAGE_NAME}:${DEFAULT_TAG}" | podman load

      # Rechunk is a script that we use on Universal Blue to make sure there isnt a single huge layer when your image gets published.
      # This does not make your image faster to download, just provides better resumability and fixes a few errors.
      # Documentation for Rechunk is provided on their github repository at https://github.com/hhd-dev/rechunk
      # You can enable it by uncommenting the following lines:
      # - name: Run Rechunker
      #   id: rechunk
      #   uses: ublue-os/legacy-rechunk@a925083d9af7cb04b3e2a6e8c01bfa495f38b710 # v1.0.0
      #   with:
      #     rechunk: 'ghcr.io/ublue-os/legacy-rechunk:v1.0.0-x86_64'
      #     ref: "localhost/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}"
      #     prev-ref: "${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}"
      #     skip_compression: true
      #     version: ${{ env.CENTOS_VERSION }}
      #     labels: ${{ steps.metadata.outputs.labels }} # Rechunk strips out all the labels during build, this needs to be reapplied here with newline separator

      # This is necessary so that the podman socket can find the rechunked image on its storage
      - name: Load in podman, tag, and compute content hash
        id: compute_hash
        run: |
          CONTENT_HASH=$(podman inspect "${IMAGE_NAME}:${DEFAULT_TAG}" \
            --format '{{json .RootFS.Layers}}' | sha256sum | cut -d' ' -f1)
          echo "Content hash: ${CONTENT_HASH}"
          echo "content_hash=${CONTENT_HASH}" >> "$GITHUB_OUTPUT"
          for tag in ${{ steps.metadata.outputs.tags }}; do
            podman tag "${IMAGE_NAME}:${DEFAULT_TAG}" "${IMAGE_NAME}:${tag}"
          done
          podman tag "${IMAGE_NAME}:${DEFAULT_TAG}" "${IMAGE_NAME}:content-${CONTENT_HASH}"

      - name: Check if content already exists in registry
        id: check_content
        shell: bash
        run: |
          TAG="content-${{ steps.compute_hash.outputs.content_hash }}"
          if skopeo inspect --raw \
            "docker://${IMAGE_REGISTRY}/${IMAGE_NAME}:${TAG}" \
            >/dev/null 2>&1; then
            echo "Content tag ${TAG} already exists. Skipping push."
            echo "should_push=false" >> "$GITHUB_OUTPUT"
          else
            echo "Content tag ${TAG} not found. Pushing."
            echo "should_push=true" >> "$GITHUB_OUTPUT"
          fi

      # These `if` statements are so that pull requests for your custom images do not make it publish any packages under your name without you knowing
      # They also check if the runner is on the default branch so that things like the merge queue (if you enable it), are going to work
      - name: Login to GitHub Container Registry
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check_content.outputs.should_push != 'false'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push To GHCR
        uses: redhat-actions/push-to-registry@5ed88d269cf581ea9ef6dd6806d01562096bee9c # v2.8
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check_content.outputs.should_push != 'false'
        id: push
        env:
          REGISTRY_USER: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ github.token }}
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }} content-${{ steps.compute_hash.outputs.content_hash }}
          username: ${{ env.REGISTRY_USER }}
          password: ${{ env.REGISTRY_PASSWORD }}

      # This section is optional and only needs to be enabled if you plan on distributing
      # your project for others to consume. You will need to create a public and private key
      # using Cosign and save the private key as a repository secret in Github for this workflow
      # to consume. For more details, review the image signing section of the README.
      - name: Install Cosign
        uses: sigstore/cosign-installer@ba7bc0a3fef59531c69a25acd34668d6d3fe6f22 # v4.1.0
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check_content.outputs.should_push != 'false'
        with:
          cosign-release: 'v2.6.3'

      - name: Sign container image
        if: github.event_name != 'pull_request' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.check_content.outputs.should_push != 'false'
        run: |
          IMAGE_FULL="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          for tag in ${{ steps.metadata.outputs.tags }} content-${{ steps.compute_hash.outputs.content_hash }}; do
            cosign sign -y --key env://COSIGN_PRIVATE_KEY $IMAGE_FULL:$tag
          done
        env:
          TAGS: ${{ steps.push.outputs.digest }}
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
